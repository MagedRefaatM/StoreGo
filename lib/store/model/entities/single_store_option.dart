import 'package:flutter/material.dart';

class SingleStoreOption {
  String name;
  IconData icon;
  int id;
  double opacityVisibility;
  double moneyAccountAmount;

  SingleStoreOption(this.name, this.icon, this.id, this.opacityVisibility,
      this.moneyAccountAmount);
}
